# Отчет по безопасности проекта Dashbord

## Дата проверки
2025-01-XX

## Критические проблемы

### 1. XSS (Cross-Site Scripting) уязвимость
**Файл:** `app/pages/region.py`  
**Строки:** 53, 123, 126-129

**Проблема:** Параметр `region` из query string декодируется и напрямую вставляется в HTML без экранирования.

```53:53:app/pages/region.py
    region = unquote_plus(region)
```

```123:129:app/pages/region.py
            html.H1(f"📊 Дашборд региона: {region}"),
            html.Div(
                [
                    html.P(f"📍 Регион: {region}"),
                    html.P(f"📈 Укомплектованность: {staffing:.1f} %"),
                    html.P(f"📈 Освоение ДС: {cash_use:.1f} %"),
                    html.P(f"📈 Исправность техники: {serviceability:.1f}%"),
```

**Риск:** Злоумышленник может внедрить JavaScript код через параметр `region` в URL, например:
```
/region?region=<script>alert('XSS')</script>
```

**Решение:** 
- Валидировать параметр `region` против whitelist допустимых регионов
- Использовать экранирование HTML (Dash автоматически экранирует строки в `html.*` компонентах, но лучше явно валидировать)

### 2. Отсутствие валидации входных данных
**Файл:** `app/pages/region.py`  
**Строки:** 46-53

**Проблема:** Параметр `region` из query string не валидируется перед использованием.

```46:53:app/pages/region.py
    # Извлекаем параметр region
    query_params = parse_qs(search.lstrip("?"))
    region = query_params.get("region", [""])[0]

    if not region:
        return html.Div("Не указан регион")

    # Декодируем имя региона
    region = unquote_plus(region)
```

**Риск:** 
- Path traversal атаки
- Инъекции через специальные символы
- DoS через очень длинные строки

**Решение:**
- Валидировать `region` против списка допустимых регионов из `gdf["name"]`
- Ограничить длину строки
- Проверить на наличие опасных символов

### 3. Запуск Docker контейнера от root пользователя
**Файл:** `Dockerfile`  
**Строки:** 1-42

**Проблема:** Контейнер запускается от пользователя root, что создает риски при компрометации.

```1:42:Dockerfile
FROM python:3.11-slim
...
CMD ["uv", "run", "uvicorn", "app.app:asgi_app", "--host", "0.0.0.0", "--port", "8000"]
```

**Риск:** При компрометации контейнера злоумышленник получает root права.

**Решение:**
- Создать непривилегированного пользователя в Dockerfile
- Запускать приложение от этого пользователя

## Высокие риски

### 4. Отсутствие CORS настроек
**Файл:** `app/app.py`

**Проблема:** Нет явной настройки CORS политики.

**Риск:** Неопределенное поведение при запросах с других доменов.

**Решение:**
- Явно настроить CORS политику в Dash приложении
- Ограничить разрешенные источники

### 5. Отсутствие rate limiting
**Проблема:** Нет защиты от DDoS атак и злоупотреблений.

**Риск:** Возможность перегрузить сервер большим количеством запросов.

**Решение:**
- Добавить rate limiting middleware (например, через nginx или uvicorn middleware)
- Настроить лимиты на количество запросов в единицу времени

### 6. Отсутствие аутентификации и авторизации
**Проблема:** Приложение полностью открыто для всех пользователей.

**Риск:** Несанкционированный доступ к данным.

**Решение:**
- Добавить аутентификацию (например, через Flask-Login или JWT)
- Настроить авторизацию для доступа к данным

## Средние риски

### 7. Отсутствие валидации размера файлов
**Файлы:** `app/services/csv_loader.py`, `app/services/geojson_loader.py`

**Проблема:** При загрузке CSV и GeoJSON файлов нет проверки их размера.

```32:32:app/services/csv_loader.py
        df = pd.read_csv(path)
```

```33:33:app/services/geojson_loader.py
    raw = gpd.read_file(path)
```

**Риск:** DoS атака через загрузку очень больших файлов.

**Решение:**
- Проверять размер файла перед чтением
- Установить максимальный размер файла

### 8. Отсутствие защиты от path traversal в методах загрузки
**Файл:** `app/services/csv_loader.py`  
**Строки:** 114-143

**Проблема:** Метод `load_csv` принимает `path: Path` без проверки, что путь находится в разрешенной директории.

```114:143:app/services/csv_loader.py
    @staticmethod  # Использование @staticmethod оправдано для утилитных методов загрузки данных
    def load_csv(  # noqa: WPS602
        path: Path,
        model: type[BaseModel],
        model_name: str | None = None,
    ) -> pd.DataFrame:
        """Универсальный метод для загрузки CSV с валидацией через Pydantic модель.

        Args:
            path: Путь к CSV файлу.
            model: Pydantic модель для валидации записей.
            model_name: Имя модели для сообщений об ошибках.
                Если не указано, используется имя класса модели.

        Returns:
            DataFrame с валидированными данными.
        """
        df = pd.read_csv(path)
```

**Риск:** Если этот метод будет вызываться с пользовательским вводом, возможен path traversal.

**Решение:**
- Проверять, что путь находится в разрешенной директории
- Использовать `path.resolve()` и проверять, что он начинается с базовой директории

### 9. Отсутствие логирования безопасности
**Проблема:** Нет логирования подозрительной активности.

**Риск:** Невозможность отследить атаки и инциденты безопасности.

**Решение:**
- Добавить логирование:
  - Неудачных попыток доступа
  - Подозрительных запросов
  - Ошибок валидации

### 10. Отсутствие HTTPS принуждения
**Проблема:** Нет принудительного использования HTTPS.

**Риск:** Перехват данных в открытом виде.

**Решение:**
- Настроить HTTPS на уровне reverse proxy (nginx)
- Добавить HSTS заголовки

## Низкие риски / Рекомендации

### 11. Отсутствие security headers
**Проблема:** Нет настройки security headers (X-Content-Type-Options, X-Frame-Options, etc.)

**Решение:**
- Добавить security headers через middleware

### 12. Отсутствие проверки зависимостей на уязвимости
**Проблема:** Нет автоматической проверки зависимостей на известные уязвимости.

**Решение:**
- Использовать инструменты типа `safety`, `pip-audit` или `dependabot`
- Регулярно обновлять зависимости

### 13. Отсутствие .env.example файла
**Проблема:** Нет примера файла с переменными окружения.

**Решение:**
- Создать `.env.example` с описанием необходимых переменных

### 14. Отсутствие secrets management
**Проблема:** Секреты могут храниться в переменных окружения без дополнительной защиты.

**Решение:**
- Использовать secrets management системы (HashiCorp Vault, AWS Secrets Manager)
- Для production использовать более безопасные методы хранения секретов

## Приоритет исправлений

1. **Критично (немедленно):** ✅ **ИСПРАВЛЕНО**
   - ✅ XSS уязвимость в `region.py` - добавлена валидация через whitelist
   - ✅ Валидация входных данных - проверка против списка допустимых регионов
   - ✅ Запуск Docker от непривилегированного пользователя - создан пользователь `appuser`

2. **Высокий приоритет:**
   - CORS настройки
   - Rate limiting
   - Аутентификация (если требуется)

3. **Средний приоритет:** ⚠️ **ЧАСТИЧНО ИСПРАВЛЕНО**
   - Валидация размера файлов
   - ✅ Защита от path traversal - добавлена в метод `load_csv`
   - Логирование безопасности

4. **Низкий приоритет:**
   - Security headers
   - Проверка зависимостей
   - Документация

## Внесенные исправления

### 1. Исправление XSS уязвимости (`app/pages/region.py`)
- Добавлен whitelist допустимых регионов (`VALID_REGIONS`)
- Параметр `region` валидируется против whitelist перед использованием
- Используется безопасное имя региона из данных, а не из пользовательского ввода
- Добавлена проверка максимальной длины входной строки (200 символов)

### 2. Исправление Docker безопасности (`Dockerfile`)
- Создан непривилегированный пользователь `appuser`
- Приложение запускается от этого пользователя вместо root
- Изменен владелец файлов приложения на `appuser`

### 3. Защита от path traversal (`app/services/csv_loader.py`)
- Добавлен опциональный параметр `base_dir` в метод `load_csv`
- Проверка, что путь находится внутри базовой директории
- Вызов исключения при попытке path traversal

## Заключение

Критические уязвимости безопасности были исправлены:
- ✅ XSS уязвимость устранена через валидацию входных данных
- ✅ Docker контейнер запускается от непривилегированного пользователя
- ✅ Добавлена защита от path traversal в методах загрузки файлов

Рекомендуется также реализовать остальные меры безопасности (CORS, rate limiting, логирование) перед развертыванием в production.
